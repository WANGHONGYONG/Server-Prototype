<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, height=device-height, initial-scale=1">

    <title>구글맵 2</title>

    <style>
        html, body {
            width: 100%;
            height: 100%;
        }

        * {
            margin: 0;
            padding: 0;
        }

        #map {
            width: 100%;
            height: 95%;
        }
    </style>

    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyCxgSpw5aHnyVE8_MhnxWji5qK8Cjl6zzQ"></script>

    <script>
        function onLoad() {
            initMap();
        }

        var map;
        var centerLocation = {lat: <%= paramLatitude %>, lng: <%= paramLongitude %>};
        var targetLocation = {lat: <%= result.geometry.coordinates[1] %>, lng: <%= result.geometry.coordinates[0] %>};

        function initMap() {

            map = new google.maps.Map(document.getElementById('map'), {
                center: centerLocation,
                zoom: 17
            });

            var myMarker = new google.maps.Marker({
                position:centerLocation,
                icon:'/public/images/mylocation.png',
                animation:google.maps.Animation.BOUNCE
            });

            myMarker.setMap(map);

            var targetMarker = new google.maps.Marker({
                position:targetLocation,
                icon:'/public/images/target2.png',
                //animation:google.maps.Animation.BOUNCE
            });

            targetMarker.setMap(map);
        }

    </script>

</head>
<body onload="onLoad()">
<a href="/public/main.html">메인화면으로 되돌아가기</a>
<p>가까운 위치 찾아 구글맵에 표시하기</p>
<div id="map"></div>

</body>
</html>